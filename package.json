{
  "name": "pagina-web",
  "private": true,
  "type": "module",
  "scripts": {
    "api-fake": "json-server --watch db.json --port3001",
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "@nuxt/devtools": "latest",
    "json-server": "^0.17.3",
    "nuxt": "^3.7.3",
    "vuetify-nuxt-module": "^0.5.12"
  },
  "dependencies": {
    "axios": "^1.5.0",
    "vuetify": "^3.3.17"
  }
}
